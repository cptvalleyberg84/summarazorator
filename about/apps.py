from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    This app handles the about page content and collaboration requests
    for the website.

    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'about'
